// For WorkOS AuthKit, we need to handle authentication differently
// since Convex doesn't natively support WorkOS AuthKit tokens
// We'll use a custom approach where we pass the user info directly
export default {
  providers: [],
};
